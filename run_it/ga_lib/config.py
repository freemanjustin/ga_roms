# config variables for the ROMS runs
# roms bin data is where the input text files live
# need full path here
roms_bin_data = "./roms_bin_data/"

# roms static data is where the forcing and bc data is
# need full path here
roms_static_data = "./roms_static_data/"
